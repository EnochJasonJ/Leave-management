// Prisma configuration - not typically needed for standard setup
// This file is kept for reference but not actively used
import "dotenv/config";

// Standard Prisma setup is handled through schema.prisma and .env file
export {};
